require 'rails_helper'

Rspec.describe "Authentication", type: :request do
  it "user can log in" do

  end

  it "once logged in, user can log out" do

  end

  it "users can't visit an admin page until they're logged in" do

  end

  it "when not logged in, user sees 'Log In' and 'Sign Up' on the navbar" do

  end

  it "once logged in, user sees 'Log Out' on the navbar" do
    
  end
end